{
  "name": "linux-forking",
  "version": "0.0.2",
  "description": "Supports a fork() call in Node.JS (only in linux and requires a patched node installation)",
  "main": "./fork.js",
  "scripts": {
    "test": "./test.js"
  },
  "dependencies": {
    "bindings": ">=1.1.1"
  },
  "repository": "",
  "author": "",
  "license": "BSD",
  "engines": {
    "node": "6.10.2-fork-support"
  }
}
